# RepoFrame 产品需求

## 产品定位

轻量、即插即用、基于代码仓库本地文件的 AI Coding 协作工具。它不直接修改业务代码，也不替代 Coding Agent，而是管理 Agent 工作所需的上下文和状态。

主要用户是使用 Coding Agent 进行日常开发、并偶尔需要持续推进复杂任务的开发者。

## 核心原则

- Local-first、Repo-native，所有持久化内容直接保存在仓库文件中。
- 不依赖数据库，不绑定特定 Coding Agent。
- `AGENTS.md` 是薄路由；稳定产品信息在初始化确认后尽量不变。
- UI 是 `.agents/` 的可视化和显式编辑层。
- 两种开发模式常驻；目标只在用户需要复杂、耗时较长任务时启用。
- 优先简单文件，不为潜在需求增加工作流或编排系统。

## 常驻模式

正常开发 `default`：依据用户当次请求，按任务选择必要验证。

小步迭代 `iteration`：优先最小改动和针对性验证，避免无关重构及不必要的全仓库检查。缩小验证范围，不跳过验证。

目标只允许在 `default` 下执行。存在当前目标时阻止切换到迭代，用户必须先暂停或完成目标。启用目标时进入正常开发。暂停或完成目标后保持正常开发，由用户决定是否切换。

## 初始化与稳定信息

`PRD.md` 保存原始需求。CLI 准备状态、路由、目录和通用初始化 Skill；现有 Agent 从已有材料提炼 `summary`、`users`、`coreRequirements`、`constraints`、`nonGoals`，展示结果并经用户一次确认后写入 `.agents/docs/product.json`。

不虚构工程规范，不调用模型 API，不覆盖已有产品信息。产品信息可在 UI 中明确编辑并保存。

## 按需目标

每个目标保存目的、完成条件、约束、进展和完成状态。支持查看、创建、编辑、启用、暂停、完成和恢复。创建默认不启用；仅一个目标可为当前目标。

暂停保留目标文件与进展，完成保留文件并标记 `completed`。Agent 根据真实工作更新进展，满足全部完成条件后可完成目标。没有目标是正常情况，不自动从普通用户请求生成目标。

目标提供跨执行恢复上下文，不保证一次 Agent 运行数小时，不自动启动 Agent。

## 交付形态

Node.js CLI + localhost Web UI，单个 npm 包。React/Vite/shadcn，官方 Base UI/nova，浅色紧凑工作台。首版全部中文。

左侧导航包含快速开始、项目和目标。快速开始仅展示项目名称，并排展示开发模式和当前目标；没有目标时保留空框架。项目页展示产品信息，按 JSON 字段动态渲染，支持增加字段，新字段默认为字符串，无需选择类型。已有数组、对象、数字等字段保留原有类型。

顶部提供全局「正常开发 / 小步迭代」切换，对应 default / iteration；连接状态位于左下路径上方，不重复显示项目名称。目标页按需管理复杂任务，不做看板和任务调度。

文件变化应刷新 UI；无效数据明确诊断，编辑冲突不静默覆盖草稿。

## 非目标

云端数据库、登录、Electron、IDE 插件、Agent SDK、Memory 系统、Workflow Engine、scheduler、runner、retry、queue、自主编排、旧 `.repoframe` 自动迁移、Git 提交按钮、目标删除、英文和主题切换。
